<%- include('partials/header') %>

<main>
    <h1><%= title %></h1>

    <h2>Dashboard</h2>
    <p>Name: <%= name %></p>
    <p>Username: <%= email %></p>

</main>

<p>
    <% if (user && user.role_name === 'admin') { %>
    <a href="/all-users">visit user page</a>
<% } %>
</p>

<%- include('partials/footer') %>